# Frontend dev server (optional — defaults shown)
PORT=5173
BASE_PATH=/

# Local Docker infrastructure — see docker/.env.example for full stack
# DATABASE_URL=postgresql://medease:medease_dev_password@localhost:5432/medease
