# Med-Ease frontend (Vite) — copy to .env.local for local dev

# NestJS API origin (no trailing slash, no /api suffix)
VITE_API_BASE_URL=http://localhost:3000

# Set to "demo" to use offline demo auth without the API (UI-only)
# VITE_AUTH_MODE=demo
