import { HelpCenterPageView } from '@/features/marketing/components/HelpCenterPageView';

/** Help center embedded inside a portal layout (patient, facility, etc.). */
export default function PortalHelpPage() {
  return <HelpCenterPageView embedded />;
}
