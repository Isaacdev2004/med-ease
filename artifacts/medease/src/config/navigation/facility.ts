import { buildPortalConfig } from '@/config/routes/build-portal-config';
import { facilityRouteGroup } from '@/config/routes/portals/facility';

export const facilityPortalConfig = buildPortalConfig(facilityRouteGroup);
