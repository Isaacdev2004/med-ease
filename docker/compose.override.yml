# Local development overrides — optional hot-reload mounts (E1-02+)
# Automatically merged when present alongside compose.yml

services:
  api:
    environment:
      NODE_ENV: development

  worker:
    environment:
      NODE_ENV: development
