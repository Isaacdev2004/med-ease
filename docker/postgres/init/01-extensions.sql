-- Med-ease local development database bootstrap (E1-01)
-- Extensions required by the enterprise data model (no Prisma migrations yet)

CREATE EXTENSION IF NOT EXISTS "pgcrypto";
CREATE EXTENSION IF NOT EXISTS "pg_trgm";
